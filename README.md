# Virtual-_paint

#Install the required libraries:

For Windows
pip install opencv-python mediapipe numpy

For macOS
pip install opencv-python mediapipe numpy

Run the application:
python Virtual_paint.py
